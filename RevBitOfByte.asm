DATA SEGMENT
    NUM DB 0B2H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

START:
    MOV AX, DATA
    MOV DS, AX

    MOV AL, NUM
    MOV CL, 8
    MOV AH,0H ;XOR AH, AH

REVERSE:
    RCL AL, 1
    RCR AH, 1
    LOOP REVERSE

    MOV AL, AH

    MOV AH, 4CH
    INT 21H

CODE ENDS
END START
